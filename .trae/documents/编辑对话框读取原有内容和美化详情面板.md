## 1. 修改编辑对话框，使其能读取原有内容

### 修改 `AddTodoDlg.h`
- 添加一个接受 `TodoItem` 参数的构造函数
- 添加一个 `SetTodoItem` 方法，用于设置任务数据

### 修改 `AddTodoDlg.cpp`
- 实现新的构造函数，使用传入的任务数据初始化
- 实现 `SetTodoItem` 方法，更新任务数据
- 修改 `OnInitDialog` 方法，根据当前任务数据初始化所有控件
- 在 `MainFrm.cpp` 的 `OnTodoEdit` 函数中，使用原任务数据初始化对话框

## 2. 美化右侧详情面板

### 修改 `MainFrame::CreateDetailPanelControls`
- 设置详情面板的背景色为浅灰色，增强视觉区分
- 添加标题栏样式，使用不同颜色或边框
- 改进字体样式，使用更清晰的字体
- 添加分隔线，增强布局层次感
- 美化空状态提示，使用更友好的样式

### 修改 `MainFrame::UpdateDetailPanel`
- 改进控件布局，使详情信息更加紧凑和美观
- 根据优先级设置不同的颜色标识
- 添加更多的任务信息，如完成状态
- 改进备注显示，支持多行文本
- 添加圆角边框或阴影效果

## 3. 具体实现细节

### 编辑对话框改进
```cpp
// 在 AddTodoDlg.h 中添加
CAddTodoDlg(const TodoItem& item);
void SetTodoItem(const TodoItem& item);

// 在 AddTodoDlg.cpp 中实现
CAddTodoDlg::CAddTodoDlg(const TodoItem& item)
    : m_item(item)
{
}

void CAddTodoDlg::SetTodoItem(const TodoItem& item)
{
    m_item = item;
}

// 在 OnInitDialog 中初始化控件
m_editTitle.SetWindowText(m_item.title.c_str());
m_editNote.SetWindowText(m_item.note.c_str());
m_comboPriority.SetCurSel((int)m_item.priority);
// 设置日期时间控件
SYSTEMTIME st = {0};
TimeToSystemTime(m_item.targetEndTime, st);
DateTime_SetSystemtime(m_dateTime, GDT_VALID, &st);
```

### 详情面板美化
```cpp
// 设置详情面板背景色
m_detailPanel.SetBkColor(RGB(245, 245, 245));

// 添加分隔线
CStatic m_detailSeparator1;
m_detailSeparator1.Create(m_detailPanel, rcDefault, NULL,
    WS_CHILD | WS_VISIBLE | SS_ETCHEDHORZ);

// 根据优先级设置颜色
if (pItem->priority == Priority::P0) {
    m_detailPriority.SetTextColor(RGB(220, 53, 69));
} else if (pItem->priority == Priority::P1) {
    m_detailPriority.SetTextColor(RGB(255, 193, 7));
} // 其他优先级...
```

这些修改将使编辑对话框能够读取原有内容，同时美化右侧详情面板，提供更好的用户体验。